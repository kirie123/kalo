---
name: expert-designer
description: 创建一个数字专家（有独立工作目录、独立记忆、独立技能、可挂定时任务的长期 Agent 实例）。当用户说「新建一个专家」「帮我做一个专门做 X 的 agent」「创建一个数字员工」时使用。
---

# expert-designer 数字专家创建引导

数字专家 = 注册表项 + 工作目录 + 目录里的 AGENTS.md 宪章。机制见
`doc/2026-08-29-digital-experts.md`：专家会话带独立记忆（`<workdir>/.kalo/memory/`）、
项目级技能（`<workdir>/.kalo/skills/`）、可挂带 `expertId` 的定时任务。

你（当前这个普通会话）的职责是**把专家规划出来并落盘**，不是替它干活。

## 四步流程

### 1. 问清使命

与用户对话澄清，一次问完，不要挤牙膏：

- **干什么**：一句话使命（写进注册表的 `mission` 与宪章开头）
- **盯什么数据**：需要什么数据源/输入（决定要不要挂 market-data、feeds、watch 任务）
- **多久跑一次**：每日/每周/手动触发（决定 cron）
- **产出给谁看**：文件沉淀在哪、要不要飞书推送

### 2. 选目录、规划结构

- 与用户确认工作目录（任意位置，如 `D:/kalo-experts/<id>/`）。创建它。
- **目录形态按使命规划，不套模板**：投资专家可能长出 `broker/ decisions/ reviews/ RULES.md`；
  论文专家可能长出 `papers/ summaries/`。规划完把结构念给用户确认。
- 固定只有两件必需品（引擎约定，不要改名）：
  - `<workdir>/.kalo/skills/`：专家专属技能
  - `<workdir>/.kalo/memory/`：专家记忆（引擎自动用，手工建空目录即可）

### 3. 写宪章 + 注册

**`<workdir>/AGENTS.md`**（专家宪章，中文），至少包含：

- 使命一句话 + 职责边界（做什么、不做什么）
- 目录约定：每个子目录/文件放什么
- 工作纪律：数据从哪来、产出写到哪、什么情况下停下来问人
- 复盘节奏（如有）：多久回看一次、复盘产物放哪

**注册**：编辑 `~/.kalo/experts.json`（不存在则新建），追加：

```json
{
  "experts": [
    {
      "id": "trader-consumer",
      "name": "消费复苏投研",
      "workdir": "D:/kalo-experts/trader-consumer",
      "mission": "…一句话使命…",
      "createdAt": "2026-08-29",
      "enabled": true
    }
  ]
}
```

`id` 规则：1-64 位字母数字/`-`/`_`，稳定不改。写之前先读现有文件，**保留已有专家，只追加**。

### 4. 配定时任务（需要才配）

编辑 `~/.kalo/agent/schedules.json` 的 `tasks` 数组，给专家的周期性工作建任务，
**每个任务带 `"expertId": "<专家id>"`**、`"cwd": "<workdir>"`：

- `watch`（零 token）：跑脚本，stdout 非空才推飞书。用于数据落盘、阈值告警、
  `broker.py verify` 这类机械活。
- `agent`（烧 token）：到点起无头专家会话跑 `prompt`。用于分析、决策、复盘。

写之前同样先读现有文件、保留已有任务。cron 是 5 字段本地时区（`分 时 日 月 周`）。

## 收尾

把规划结果汇总给用户确认：目录结构、宪章要点、定时任务表。用户点头后，
告诉TA去「数字专家」面板查看，或直接在面板里「开始会话」给专家下达第一条指令。

## 不做

- 不替专家执行它的日常工作（那是专家自己的会话/定时任务的事）。
- 不在专家目录之外沉淀专家的工作产出。
- 投资类专家需要虚拟账号时，引导用户使用 `paper-broker` skill（账本放 `<workdir>/broker/`）。
