{
  "name": "kalo",
  "private": true,
  "version": "0.2.0",
  "description": "AI coding agent desktop client, built on the pi agent engine (https://github.com/earendil-works/pi)",
  "workspaces": [
    "kalo-desktop"
  ],
  "scripts": {
    "setup": "cd kalo-harness && npm ci --ignore-scripts && npm run build && cd ../kalo-desktop && npm install",
    "dev": "bun scripts/ensure-engine.mjs && bun run --cwd kalo-desktop tauri dev",
    "build": "bun scripts/clean-bundle.mjs && bun scripts/ensure-engine.mjs && bun run --cwd kalo-desktop tauri build",
    "build:engine": "bash scripts/build-engine.sh",
    "version:show": "bun scripts/version.mjs",
    "version:set": "bun scripts/version.mjs",
    "version:patch": "bun scripts/version.mjs patch",
    "version:minor": "bun scripts/version.mjs minor",
    "version:major": "bun scripts/version.mjs major"
  }
}
